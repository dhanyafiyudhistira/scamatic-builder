MONGO_URI=mongodb+srv://USER:PASSWORD@HOST/DATABASE
# API cold-start recovery. The HTTP listener stays alive while readiness
# remains unavailable. 0 attempts means retry indefinitely.
API_MONGO_STARTUP_MAX_ATTEMPTS=0
API_MONGO_RETRY_INITIAL_MS=500
API_MONGO_RETRY_MAX_MS=10000
# Readiness returns HTTP 503 within this deadline while connection continues.
MONGO_READINESS_TIMEOUT_MS=2500

# Worker cold-start recovery. 0 attempts means retry indefinitely, with the
# delay capped by CONNECTOR_MONGO_RETRY_MAX_MS.
CONNECTOR_MONGO_STARTUP_MAX_ATTEMPTS=0
CONNECTOR_MONGO_RETRY_INITIAL_MS=500
CONNECTOR_MONGO_RETRY_MAX_MS=10000

# Dedicated Chart telemetry storage. Use a different MongoDB cluster from
# MONGO_URI in production so telemetry traffic cannot exhaust the control plane.
CHART_MONGO_URI=mongodb+srv://USER:PASSWORD@TELEMETRY_HOST/scamatic_telemetry
CHART_MONGO_DB=scamatic_telemetry
CHART_MONGO_COLLECTION=chart_samples
CHART_MONGO_RETENTION_DAYS=30
CHART_MONGO_BATCH_SIZE=500
CHART_MONGO_FLUSH_MS=250
CHART_MONGO_MAX_QUEUE=20000
CHART_MONGO_MAX_POOL_SIZE=20
CHART_HISTORY_MAX_BOOTSTRAP_POINTS=10000
# Required in production. The hostname in the write-only URI must be listed.
CHART_MONGO_ALLOWED_HOSTS=telemetry.example.mongodb.net
# Optional explicit exception for an approved private telemetry hostname.
CHART_MONGO_ALLOWED_PRIVATE_HOSTS=
# Development escape hatch only. Never enable this in production.
CHART_MONGO_ALLOW_SHARED_CLUSTER=false

# Required only until the production owner is bootstrapped. Change the password
# through the application, then clear SCADA_ADMIN_PASSWORD and restart.
SCADA_ADMIN_EMAIL=admin@scada.local
SCADA_ADMIN_PASSWORD=replace-with-a-strong-password
SCADA_WORKSPACE_ID=default
SCADA_SESSION_TTL_SECONDS=43200
SCADA_SESSION_IDLE_SECONDS=1800
SCADA_MAX_AUTH_SESSIONS=10
SCADA_PROJECT_UNLOCK_SECONDS=28800
APP_ORIGIN=https://scada-dhany-wtp.vercel.app
# Google OAuth web application credentials. Keep GOOGLE_CLIENT_SECRET on the
# server only; VITE_GOOGLE_AUTH_URL is intentionally browser-visible.
GOOGLE_CLIENT_ID=replace-with-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=replace-with-google-client-secret
GOOGLE_REDIRECT_URI=replace-with-your-own-redirect-url
# For local development, replace APP_ORIGIN and GOOGLE_REDIRECT_URI with:
# APP_ORIGIN=http://localhost:5173
# GOOGLE_REDIRECT_URI=http://localhost:5173/api/auth/callback/google
# The same-origin start path works in both local and production deployments.
# The encoded `next` query controls the page opened after authentication.
VITE_GOOGLE_AUTH_URL=/api/auth/google/start?next=%2F

# Connector platform. Keep both feature flags off until staging is signed off.
CONNECTOR_PLATFORM_ENABLED=false
CONNECTOR_LIVE_COMMANDS_ENABLED=false
# Vercel defaults to serverless; persistent worker deployments default to worker.
CONNECTOR_EXECUTION_MODE=serverless
CONNECTOR_POLL_INTERVAL_MS=2000
LEGACY_DIRECT_THINGSBOARD_ENABLED=false
CONNECTOR_ENVIRONMENT=staging
# embedded keeps UI, REST, and /runtime-stream on the Express port and starts
# the Node connector worker as a private child process. Use standalone only for
# compatibility with deployments that still expose a dedicated stream port.
CONNECTOR_STREAM_MODE=embedded
CONNECTOR_STREAM_PORT=3002
# Optional override for standalone/external stream deployments. Leave empty in
# embedded mode so the WebSocket URL is derived from APP_ORIGIN.
CONNECTOR_STREAM_PUBLIC_URL=
# Serve the Vite production build from Express after running npm run build.
SERVE_STATIC_FRONTEND=true
# Loopback is the secure default. Use 0.0.0.0 only for an explicitly secured LAN deployment.
SCAMATIC_BIND_HOST=127.0.0.1
# Phase 2 opt-in. The Rust/Tokio/Axum process observes mirrored events only;
# Node remains the sole active telemetry/RPC executor.
SCADA_RUST_SHADOW_ENABLED=false
# Optional prebuilt binary path. Development falls back to the Cargo workspace target.
SCADA_RUST_SHADOW_BINARY=
# Phase 3 canary. This global kill switch and a public stream URL are required.
# Workspace administrators select eligible projects from Settings → Isaac
# runtime setup. Axum stays on loopback; expose only /isaac-stream at the edge.
SCADA_ISAAC_CANARY_ENABLED=false
SCADA_ISAAC_STREAM_BIND=127.0.0.1:3003
SCADA_ISAAC_STREAM_PUBLIC_URL=
# Connected Isaac sessions are re-authorized by Node/Mongo at this interval.
SCADA_ISAAC_REVALIDATE_MS=5000

# Tauri Desktop Connected. Compile release installers with the HTTPS control-plane
# origin below. Runtime overrides are accepted only for HTTPS or loopback HTTP.
SCAMATIC_DESKTOP_SERVER_ORIGIN=https://scada-dhany-wtp.vercel.app
# Worker-mode latest-value persistence. Runtime WebSocket delivery stays on the
# live path while snapshots and last-event health are coalesced in bounded queues.
CONNECTOR_SNAPSHOT_BATCH_SIZE=500
CONNECTOR_SNAPSHOT_FLUSH_MS=100
CONNECTOR_SNAPSHOT_MAX_PENDING=20000
CONNECTOR_HEALTH_BATCH_SIZE=100
CONNECTOR_HEALTH_FLUSH_MS=5000
CONNECTOR_HEALTH_MAX_PENDING=10000
# Command-health metadata is deferred so terminal RPC status can be pushed as
# soon as its durable CommandEvent update succeeds.
CONNECTOR_COMMAND_HEALTH_BATCH_SIZE=100
CONNECTOR_COMMAND_HEALTH_FLUSH_MS=100
CONNECTOR_COMMAND_HEALTH_MAX_PENDING=10000
CONNECTOR_TERMINAL_AUDIT_ACTIVE_MS=1000
CONNECTOR_TERMINAL_AUDIT_IDLE_MS=30000
# Embedded mode wakes the managed worker immediately after durable command
# authorization. Polling remains active as a loss-tolerant fallback; set this
# to false for an instant rollback to polling-only dispatch.
CONNECTOR_COMMAND_WAKE_ENABLED=true
CONNECTOR_COMMAND_POLL_MS=250
# Safe structured phase durations only; payloads and credentials are excluded.
CONNECTOR_RPC_TIMING_LOGS=false
CONNECTOR_RPC_METRICS_CAPACITY=1000
# Published versions are immutable. The worker keeps a bounded LRU so repeated
# commands do not reload the same schema from MongoDB on every dispatch batch.
CONNECTOR_COMMAND_VERSION_CACHE_MAX_ENTRIES=32
CONNECTOR_COMMAND_VERSION_CACHE_TTL_MS=300000
COMMAND_VERSION_CACHE_MAX_ENTRIES=8
COMMAND_VERSION_CACHE_TTL_MS=60000
# RPC retention is fail-closed and disabled by default. Before enabling it, run
# `npm run audit:rpc-retention` and explicitly create the partial purgeAt index
# with `npm run migrate:rpc-retention -- --apply` during a quiet period.
# Optionally scope the audit with RPC_RETENTION_AUDIT_PROJECT_ID.
RPC_RETENTION_AUDIT_PROJECT_ID=
COMMAND_RETENTION_ENABLED=false
COMMAND_RETENTION_ACK_DAYS=90
COMMAND_RETENTION_FAILURE_DAYS=90
COMMAND_RETENTION_BATCH_SIZE=100
COMMAND_RETENTION_INTERVAL_MS=600000
COMMAND_RETENTION_INITIAL_DELAY_MS=60000
COMMAND_RETENTION_LEASE_MS=120000
COMMAND_RETENTION_QUERY_MAX_TIME_MS=5000
# Recheck state-protected acknowledged commands monthly instead of scanning
# them every janitor cycle when no durable TagValueSnapshot exists yet.
COMMAND_RETENTION_STATE_RECHECK_MS=2592000000
SCADA_RUNTIME_SESSION_SECONDS=1800
SCADA_STREAM_TICKET_SECONDS=60
SCADA_MAX_RUNTIME_SESSIONS=5
# One runtime per ThingsBoard device owns RPC polling at a time; lifecycle records retain
# enough state to resume telemetry/response delivery after a transient failure.
SIMULATION_RESPONDER_LEASE_MS=35000
SIMULATION_RPC_RETENTION_MS=86400000
# 32 random bytes encoded as base64. Never commit the real value.
SCADA_CONNECTOR_MASTER_KEY=
# Rotation only: comma-separated previous keys. Clear after rewrap verification.
SCADA_CONNECTOR_PREVIOUS_MASTER_KEYS=
# Required in production to prevent connector SSRF (comma-separated hostnames).
CONNECTOR_ALLOWED_HOSTS=thingsboard.example.com
# Optional explicit exception for approved private ThingsBoard hostnames.
CONNECTOR_ALLOWED_PRIVATE_HOSTS=

# Optional project scope for the isolated legacy telemetry history screen.
VITE_LEGACY_PROJECT_ID=
