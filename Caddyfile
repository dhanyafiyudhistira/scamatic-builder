:8088 {
	encode zstd gzip

	# Isaac remains loopback-only. Caddy exposes only its authenticated stream;
	# Express continues to own the frontend, REST API, and Standard WebSocket.
	handle /isaac-stream {
		reverse_proxy 127.0.0.1:3003
	}

	handle {
		reverse_proxy 127.0.0.1:3001
	}
}
