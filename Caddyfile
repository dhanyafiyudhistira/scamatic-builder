:8088 {
	encode zstd gzip

	# Optional TLS/edge layer. Express owns the frontend, REST API, and WebSocket
	# upgrade on one origin; Caddy is not required for local/LAN installation.
	reverse_proxy 127.0.0.1:3001
}
