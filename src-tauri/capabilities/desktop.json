{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-connected",
  "description": "Minimal permissions for the bundled SCAMATIC Builder and Runtime webview.",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": [
    "core:default",
    "core:webview:allow-set-webview-zoom"
  ]
}
